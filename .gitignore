# Sphinx
docs/_build/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
PIPFILE.lock

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs and editors
.idea
.vscode
*.sw?

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
*.db
*.sqlite
*.sqlite3
test_*.db
temp_*.db
malicious/

# Logs
*.log
logs
dev-debug.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node and Visual Studio leftovers from the template this file started as. There
# is no JavaScript in this repository and no solution file; these are kept so a
# stray artifact stays out of the tree, not because the project has either.
node_modules/
*.suo
*.ntvs*
*.njsproj
*.sln

# Development directories
.cursor/
.claude/

# Generated assets and build artifacts (but allow assets/logo.png)
/assets/icons/
/assets/logos/
favicon.ico
logo*.png
logo*.svg
!assets/logo.png

# Temporary and test files
test_*.txt
test_*.csv
test_*.json
cleanup_*.txt
large_file_test_*.txt
*_test_*/
test_env/
test_data_*

# Development documentation
*COVERAGE*.md
*RELEASE_NOTES*.md
*VALIDATION*.md
*PYPI_PUBLISHING*.md
*github_finalization*.md
*INFLUXDB_FIX*.md
*LOGO_DESIGN*.md

# Submission files (keep local copies only)
docker-mcp-submission.yaml
submission-instructions.md

# Validation scripts (keep local if needed)
validate_synchronous.py
validate_test_fixes.py

# Unused API configuration
.env.example

# Downloaded test datasets
tests/datasets/

# Oracle datasets provisioned by scripts/build_oracle_datasets.py --out
# (the committed manifest + .xls fixture are tracked; the CSVs are not).
oracle-datasets/

# Generated test fixtures
tests/fixtures/

# TLS material for the endpoint harness, generated by the
# `localdata-test-tls-ca` compose service on every `up`. A private key is never
# committed, not even a throwaway one for a test container — a key in a public
# history is a key, whatever it was made for.
tests/tls/

# Task files
tasks.json
tasks/ 
