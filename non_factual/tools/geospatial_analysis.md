<!-- MACHINE-WRITTEN by localdata_mcp.nexus.contract.generators.docs — DO NOT EDIT; regenerate via `python -m localdata_mcp.nexus.contract.generate` -->

# Tools — geospatial_analysis

| Tool | Summary | Input shape | Output shape | Streaming | Params |
|---|---|---|---|---|---|
| `check_geospatial_capabilities` | Report which geospatial backend libraries are installed and what the geospatial extra enables — answers unconditionally. | NONE | SCALAR | no | — |
| `analyze_spatial_autocorrelation` | Global Moran's I on an addressed point source: whether nearby locations hold similar value_column values, over a k-nearest-neighbour neighbourhood. Reports I, z-score, and p-value. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `value_column`, `x_column?`, `y_column?`, `k_neighbors?` |
| `find_spatial_hotspots` | Getis-Ord Gi* hot- and cold-spots on an addressed point source: the statistically significant clusters of high or low value_column at the significance level. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `value_column`, `x_column?`, `y_column?`, `significance_level?` |
| `calculate_spatial_distances` | Summarize pairwise Euclidean distances between the points of an addressed source (min/max/mean/median), bounded by point count to keep the matrix legible. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `x_column?`, `y_column?` |
| `perform_spatial_join` | Attach the addressed source's rows to the inline right_geometries they spatially relate to (predicate intersects (default), within, or contains). The addressed source's geometry_column holds WKT. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `geometry_column`, `right_geometries`, `predicate?` |
| `perform_spatial_overlay` | Set operation (intersection (default), union, difference, symmetric_difference) between the addressed source's WKT geometry_column and the inline right_geometries. | TABULAR | GEO | no | `endpoint?`, `path?`, `table?`, `query?`, `geometry_column`, `right_geometries`, `operation?` |
| `aggregate_points_in_polygons` | Summarize an addressed point source's value_column inside each inline polygon (WKT list): mean/sum/count by default. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `value_column`, `polygons`, `x_column?`, `y_column?`, `aggregations?` |
| `optimize_route` | Order waypoints greedily and connect them by shortest path over a network whose nodes are the addressed source (id, x, y) and whose edges arrive inline. Reports the order, path, and total distance. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `edges`, `waypoints`, `node_id_column?`, `x_column?`, `y_column?`, `return_to_start?` |
| `analyze_accessibility` | Score demand nodes by travel time to their nearest service node over a network (addressed nodes, inline edges), optionally capped at max_travel_time. | TABULAR | SCALAR | no | `endpoint?`, `path?`, `table?`, `query?`, `edges`, `service_locations`, `demand_locations`, `node_id_column?`, `x_column?`, `y_column?`, `max_travel_time?` |
| `generate_service_isochrones` | The node set reachable from the service nodes within each travel-time band, with its convex-hull footprint, over a network (addressed nodes, inline edges). | TABULAR | GEO | no | `endpoint?`, `path?`, `table?`, `query?`, `edges`, `service_locations`, `time_bands`, `node_id_column?`, `x_column?`, `y_column?` |
