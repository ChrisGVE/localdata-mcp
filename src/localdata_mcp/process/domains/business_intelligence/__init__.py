"""localdata_mcp/process/domains/business_intelligence — FR-301's BI family.

Two tools (tools.py declares them): `analyze_rfm` — carried by name
from `main` with the FR-307 cascade reorder (every documented segment
reachable) — and `calculate_clv`, FIRST registered in v3 (unregistered
orphan on `main` per S9.1; FR-309 makes the customer column a real
parameter instead of a hardcoded name).
"""
