"""localdata_mcp/process/domains/regression_modeling — FR-301's regression family.

Two tools carried by name from `main` (tools.py declares them):
`analyze_regression` (fit + fit-quality with the sentinel's
rank-deficiency keys, FR-306's parameter passthrough) and
`evaluate_model_performance` (actual-vs-predicted scoring).
"""
