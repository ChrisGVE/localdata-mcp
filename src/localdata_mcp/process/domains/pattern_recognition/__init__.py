"""localdata_mcp/process/domains/pattern_recognition — FR-301's pattern family.

Four tools carried by name from `main` (tools.py declares them):
`analyze_clusters`, `detect_anomalies`, `reduce_dimensions` (FR-308's
explained-variance closure), and `transform_data` (regex column
rewrite through the safe-pattern screen).
"""
