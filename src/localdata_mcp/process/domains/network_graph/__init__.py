"""localdata_mcp/process/domains/network_graph — FR-301's network family.

One tool carried by name from `main` (tools.py declares it):
`analyze_network` over a tabular edge list — the I-3 graph-store
tools (`get_edges` etc.) are its data surface through composition.
"""
