"""Streaming execution subpackage (E11.1, S5.4 pre-split): the chunked
per-stage loop (executor.py), the stable variance-class combiners
(combiners.py — C-1's Welford/Chan constraint), and the sklearn
partial_fit adapter (sklearn_adapter.py)."""
